riot file-import https://storage.googleapis.com/jrx/lacity.csv --date "MM/dd/yyyy HH:mm:ss a" --process EventStartDate="remove('Event Start Date')" EpochStart="#date.parse(EventStartDate).getTime()" index="#index" --header hset --keyspace event --keys Id