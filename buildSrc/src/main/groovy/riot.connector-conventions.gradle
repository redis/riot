plugins {
    id 'net.researchgate.release'
    id 'org.asciidoctor.jvm.convert'
    id 'application'
    id 'java-library'
    id 'maven-publish'
    id 'signing'
}

repositories {
    mavenCentral()
}

tasks.withType(Tar) {
    compression = Compression.GZIP
}