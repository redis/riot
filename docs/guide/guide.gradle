asciidoctor {
    resources {
        from("../../connectors/riot-file/src/test/resources/files") {
            include '*.*'
        }
    }
    outputDir = '../../build/docs/asciidoc/'
}

eclipse {
    project {
        name = 'riot-guide'
    }
}
