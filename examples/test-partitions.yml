flows:
- name: partitions
  max-item-count: 40
  partitions: 20
  generator:
    fields:
      id: "#context.nextId(100,'%02d')"
  redis:
  - search:
      keyspace: test
      keys: id
      schema:
      - name: id
        sortable: true
      